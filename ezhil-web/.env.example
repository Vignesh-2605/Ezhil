# Backend API base URL.
# Leave empty in dev — vite.config.ts proxies /api to http://localhost:8080.
# Set to the deployed origin for production builds, e.g.
#   VITE_API_URL=https://api.ezhil.example.org
VITE_API_URL=
