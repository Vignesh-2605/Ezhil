node_modules/

# Run output. These are regenerated by every run and published from CI as
# build artifacts and to Pages, so committing them would only ever produce
# conflicts over which run's numbers are in the tree.
reports/*.json
reports/*.xlsx
reports/*.html
# ...except the generator itself.
!reports/build-report.js
