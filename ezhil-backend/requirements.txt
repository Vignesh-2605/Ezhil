fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy[asyncio]>=2.0.35
aiosqlite==0.20.0
alembic==1.13.1
pydantic>=2.9.0
pydantic-settings==2.2.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.9
pillow>=10.4.0

# ── OCR ──────────────────────────────────────────────────────────────────────
easyocr>=1.7.1

# ── Multi-file extraction (NEW) ───────────────────────────────────────────────
pymupdf>=1.24.0        # PDF text extraction + page rendering  (import fitz)
python-docx>=1.1.0     # DOCX paragraph / table extraction

# ── SLM / LLM ────────────────────────────────────────────────────────────────
# Install torch separately on Windows if needed:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
transformers>=4.40.0
huggingface_hub>=0.23.0

# Optional — fast GGUF inference (if ezhil-gemma4-q4_k_m.gguf is present):
#   pip install llama-cpp-python
# Optional — LoRA adapter loading:
#   pip install peft

# ── Tests ────────────────────────────────────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0          # ASGITransport, used by tests/conftest.py
